<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Warm Glow Theme - Dark burgundy + golden accent -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#1A0F13"/>

        <RadialGradientBrush x:Key="MessageListBrush"
                     GradientOrigin="1,0"
                     Center="1,0"
                     RadiusX="2" RadiusY="2">
    
    <GradientStop Color="#F2C4C4" Offset="0.0"/>
    <GradientStop Color="#FFFFFF" Offset="0.9"/>
    </RadialGradientBrush>

    <SolidColorBrush x:Key="LeftPanelBrush" Color="#26171D"/>
    <SolidColorBrush x:Key="ChatListBrush" Color="#341D26"/>
    <SolidColorBrush x:Key="MessageInputBrush" Color="#26171D"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#E0A84D"/>
    <Color x:Key="AccentColor">#E0A84D</Color>
    <SolidColorBrush x:Key="ForegroundPrimaryBrush" Color="#F1E8E8"/>
    <SolidColorBrush x:Key="ForegroundSecondaryBrush" Color="#BFAFB2"/>
    <SolidColorBrush x:Key="ForegroundTetriaryBrush" Color="#3E3434"/>
    <SolidColorBrush x:Key="BorderBrushColor" Color="#341D26"/>
    <Color x:Key="BorderColor">#341D26</Color>
    <SolidColorBrush x:Key="RecieveBubbleBrush" Color="#E8E0D5"/>
    <SolidColorBrush x:Key="SendBubbleBrush" Color="#D9A6A6"/>  
    
    <!-- Message text colors for dark theme -->
    <SolidColorBrush x:Key="ReceivedMessageTextBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SentMessageTextBrush" Color="#FFFFFF"/>

    <SolidColorBrush x:Key="DeleteButtonBrush" Color="#E1B6B6" Opacity="0.60"/>
    <SolidColorBrush x:Key="DeleteTextBrush" Color="#8C5E5E"/>

    <Style x:Key="RoundButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="14"/>
        <Setter Property="Height" Value="14"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="18" ShadowDepth="4" Color="#22000000" Opacity="0.18"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ToggleIconStyle" TargetType="RadioButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="#8C7A80"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid x:Name="GridRoot" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="GridRoot" Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#F1E8E8"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="GridRoot" Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#F1E8E8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

